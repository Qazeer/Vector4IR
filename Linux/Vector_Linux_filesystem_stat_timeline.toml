# Description: Process Linux filesystem stat timeline.
#              Filename should match: "*stat_timeline*.log".
#              Timeline can be generated with: find <DIRECTORY> -xdev -print0 | xargs -0 stat -c 'crtime="%w" crtime_epoch="%W" mtime="%y" mtime_epoch="%Y" ctime="%z" ctime_epoch="%Z" atime="%x" atime="%X" size_bytes="%s" userID="%u" username="%U" groupID="%g" groupname="%G" access="%a" access_pretty="%A" filetype="%F" filename="%n" filename_deref="%N"'
# Author(s): Thomas DIOT (_Qazeer)
# Version: 1.0
# Last modified: 2022-09-01
# Required environment variables:
#   INPUT_FOLDER = "<INPUT_FOLDER>"
#   VECTOR_TEMP_FOLDER = "<VECTOR_TEMP_FOLDER>"
#   INDEX = "<INDEX>"
#   SOURCE_HOST = "<SOURCE_HOST>"
#   -- ELK sink specific variables. ELK index = <INDEX> & source = linux_stat_timeline & sourcetype = "linux_artefact".
#   ELK_IP = "<IP>"
#   ELK_PORT = "<PORT>" -- ELK bulk HTTP API port. Defaults to 9200.
#   ELK_USERNAME = "<USERNAME>"
#   ELK_PASSWORD = "<PASSWORD>"
#   -- Splunk sink specific variables. Splunk index = <INDEX> & source = linux_stat_timeline & sourcetype = "linux_artefact".
#   SPLUNK_IP = "<IP>"
#   SPLUNK_PORT = "<PORT>" -- Splunk HTTP Event Collector port. Defaults to 8088.
#   SPLUNK_HEC_TOKEN = "<SPLUNK_HEC_TOKEN>" -- Splunk HEC token: https://docs.splunk.com/Documentation/Splunk/9.0.1/Data/UsetheHTTPEventCollector.

# Temporary folder used by Vector.
data_dir = "${VECTOR_TEMP_FOLDER:?A directory for Vector temporary data is required}"

[sources.Linux_stat_timeline_input]
  type = "file"
  ignore_checkpoints = true
  include = ["${INPUT_FOLDER:?The input folder containing the Linux stat timeline files is required}/*stat_timeline*.log"]
  read_from = "beginning"
  oldest_first = true
  fingerprint.strategy = "device_and_inode"
  max_line_bytes = 100_000_000

[transforms.Linux_stat_timeline_parser]
  type = "remap"
  inputs = ["Linux_stat_timeline_input"]
  source = '''
    # Parse the key-value pairs.
    ., err = parse_key_value(.message, key_value_delimiter: "=", field_delimiter: " ")   
    if (err != null) {
      log("Unable to convert to parse log line: " + err, level: "error")
    }

    # Coerce types.
    . = map_values(.) -> |value| {
      if (tmp_value, err = parse_timestamp(value, format: "%Y-%m-%d %H:%M:%S.%f %z"); err == null) {
        tmp_value
      } else if (tmp_value, err = parse_int(value); err == null) {
        tmp_value
      } else if (tmp_value, err = to_bool(value); err == null) {
        tmp_value
      } else {
        value
      }
    }
   
    # Add the metadata fields following the parsing.
    .sourcehost = "${SOURCE_HOST:?The source host is required}"
    .host = .sourcehost
    .source = "linux_stat_timeline"
    .sourcetype = "linux_artefact"
  '''

[transforms.Linux_stat_timeline_dedup_crtime]
  type = "remap"
  inputs = ["Linux_stat_timeline_parser"]
  source = '''
    if !(exists(.crtime) && is_timestamp(.crtime)) {
      abort
    }
    .timestamp = .crtime
    .operation = "creation"
  '''

[transforms.Linux_stat_timeline_dedup_mtime]
  type = "remap"
  inputs = ["Linux_stat_timeline_parser"]
  source = '''
    if !(exists(.mtime) && is_timestamp(.mtime)) {
      abort
    }
    .timestamp = .mtime
    .operation = "modification"
  '''

[transforms.Linux_stat_timeline_dedup_ctime]
  type = "remap"
  inputs = ["Linux_stat_timeline_parser"]
  source = '''
    if !(exists(.ctime) && is_timestamp(.ctime)) {
      abort
    }
    .timestamp = .ctime
    .operation = "change"
  '''

[transforms.Linux_stat_timeline_dedup_atime]
  type = "remap"
  inputs = ["Linux_stat_timeline_parser"]
  source = '''
    if !(exists(.atime) && is_timestamp(.atime)) {
      abort
    }
    .timestamp = .atime
    .operation = "access"
  '''

# Sink to send processed logs to void, for validating that the pipeline doest not raise errors.
# [sinks.Linux_stat_timeline_blackhole_sink]
#   type = "blackhole"
#   inputs = [
#     "Linux_stat_timeline_dedup_crtime",
#     "Linux_stat_timeline_dedup_mtime",
#     "Linux_stat_timeline_dedup_ctime",
#     "Linux_stat_timeline_dedup_atime"
#   ]

# Sink to send processed logs to the console, for debugging.
# [sinks.Linux_stat_timeline_console_sink]
#   type = "console"
#   inputs = [
#     "Linux_stat_timeline_dedup_crtime",
#     "Linux_stat_timeline_dedup_mtime",
#     "Linux_stat_timeline_dedup_ctime",
#     "Linux_stat_timeline_dedup_atime"
#   ]
#   target = "stdout"
#   encoding.codec = "json"
  
# Sink transform to send processed logs to an ELK instance.
# [sinks.Linux_stat_timeline_ELK_sink]
#   type = "elasticsearch"
#   inputs = [
#     "Linux_stat_timeline_dedup_crtime",
#     "Linux_stat_timeline_dedup_mtime",
#     "Linux_stat_timeline_dedup_ctime",
#     "Linux_stat_timeline_dedup_atime"
#   ]
#   mode="bulk"
#   # bulk.action = "create"
#   bulk.index = "${INDEX:?An index name must be specified for ELK / Splunk index}"
#   endpoint = "http://${ELK_IP:?ELK IP is required}:${ELK_PORT:-9200}"
#   auth.strategy = "basic"
#   auth.user = "${ELK_USERNAME:?ELK_USERNAME is required}"
#   auth.password = "${ELK_PASSWORD:?ELK_PASSWORD is required}"
#   suppress_type_name = true # type field in header is deprecated since ELK 7.x and has been removed from ELK 8.x

# Sink transform to send processed logs to a Splunk instance.
# [sinks.Linux_stat_timeline_Splunk_sink]
#   type = "splunk_hec_logs"
#   inputs = [
#     "Linux_stat_timeline_dedup_crtime",
#     "Linux_stat_timeline_dedup_mtime",
#     "Linux_stat_timeline_dedup_ctime",
#     "Linux_stat_timeline_dedup_atime"
#   ]
#   endpoint = "http://${SPLUNK_IP:?Splunk IP is required}:${SPLUNK_PORT:-8088}"
#   default_token = "${SPLUNK_HEC_TOKEN:?Splunk HEC token is required}"
#   compression = "none"
#   index = "${INDEX:?An index name must be specified for the ELK / Splunk index}"
#   host_key = "host"
#   source = "{{ source }}"
#   sourcetype = "{{ sourcetype }}"
#   tls.verify_certificate = false
#   encoding.codec = "json"