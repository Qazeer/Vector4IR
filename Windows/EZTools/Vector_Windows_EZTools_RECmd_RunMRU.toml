# Vector_Windows_EZTools_RECmd_RunMRU config.
# Description: Process Windows artefacts parsed with RECmd (CSV output).
#              Filename should match: "*RunMRU*.csv"
#              Hive type: NTUSER
#              Registry key path: Software\Microsoft\Windows\CurrentVersion\Explorer\RunMRU
#              RECmd command: RECmd\RECmd.exe -d %sourceDirectory% --bn RECmd\RECmd_Batch_MC.reb --nl --csv %destinationDirectory% -q
#              Example command: RECmd\RECmd.exe -d "C:\" --bn RECmd\RECmd_Batch_MC.reb --nl --csv <YOUR_OUTPUT> -q
#              source = "windows_registry_explorer_runmru" & sourcetype = "windows_artefact"
# Author(s): Jérôme BREBION, Thomas DIOT (_Qazeer)
# Version: 1.0
# Last modified: 2022-09-28
# Required environment variables:
#   INPUT_FOLDER = "<INPUT_FOLDER>"
#   VECTOR_TEMP_FOLDER = "<VECTOR_TEMP_FOLDER>"
#   SOURCE_HOST = "<SOURCE_HOST>"

# Temporary folder used by Vector.
data_dir = "${VECTOR_TEMP_FOLDER:?A directory for Vector temporary data is required}"

[sources.RECmd_RunMRU_csv_input]
  type = "file"
  include = ["${INPUT_FOLDER:?The input folder containing the RECmd Registry RunMRU files is required. Add '**' to process the specified folder recursively (example: \"C:\\logs\" or folder \"C:\\logs\\**\")}/**/*RunMRU*.csv"]
  data_dir = "${VECTOR_TEMP_FOLDER:?A directory for Vector temporary data is required}"
  ignore_checkpoints = true
  read_from = "beginning"
  oldest_first = true
  fingerprint.strategy = "device_and_inode"
  max_line_bytes = 100_000_000

[transforms.RECmd_RunMRU_csv_parser]
  type = "remap"
  inputs = ["RECmd_RunMRU_csv_input"]
  source = '''
    # Expect CSV header:
    # BatchKeyPath,BatchValueName,ValueName,MruPosition,Executable,OpenedOn
    fields, err = parse_csv(.message, delimiter: ",")
    if err != null {
      log("[RunMRU] Unable to split CSV: " + err, level: "error")
    }

    if fields[0] == "BatchKeyPath" {
      expected_header = "BatchKeyPath,BatchValueName,ValueName,MruPosition,Executable,OpenedOn"
      current_header = strip_whitespace(string!(.message))
      if expected_header != current_header {
        log("Unexpected CSV header, possible non-integrated tool update!\n=> Expected CSV header: " + expected_header + "\n=> Got CSV header:      " + current_header, level: "error")
      }
      abort
    }

    .batchkeypath = fields[0]
    .batchvaluename = fields[1]
    .mruposition = fields[3]
    .executable = fields[4]
    .openedon = fields[5]

    .timestamp = .openedon

    if (match_user_en, err = parse_regex(.file, r'[a-zA-Z]_Users_.*_Users_(?P<username>.*)_NTUSER.DAT.csv'); err == null) {
      .user = match_user_en.username
    } else if (match_user_fr, err = parse_regex(.file, r'[a-zA-Z]_Utilisateurs_.*_Utilisateurs_(?P<username>.*)_NTUSER.DAT.csv'); err == null) {
      .user = match_user_fr.username
    } else {
      .user = ""
    }

    # Add the metadata fields following the parsing.
    .sourcehost = "${SOURCE_HOST:?The source host is required}"
    .host = .sourcehost
    .source = "windows_registry_explorer_runmru"
    .sourcetype = "windows_artefact"

    # Delete irrelevant default fields.
    del(.source_type)
    del(.file)
    del(.message)
  '''

[transforms.RECmd_RunMRU_coerce_to_sink]
  type = "remap"
  inputs = ["RECmd_RunMRU_csv_parser"]
  source = '''
    . = map_values(.) -> |value| {
      if (tmp_value, err = parse_timestamp(value, format: "%Y-%m-%d %H:%M:%S"); err == null) {
        tmp_value
      } else if (tmp_value, err = parse_timestamp(value, format: "%Y-%m-%d %H:%M:%S.%f"); err == null) {
        tmp_value
      } else if (tmp_value, err = parse_int(value); err == null) {
        tmp_value
      } else if (tmp_value, err = to_bool(value); err == null) {
        tmp_value
      } else {
        value
      }
    }
  '''
