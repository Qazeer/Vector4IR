# Description: Process VMware ESXi shell logs.
#              Filename should match: "*shell.log*"
#              Default path on disk: /var/run/log/shell.log*
# Author(s): Thomas DIOT (_Qazeer)
# Version: 1.0
# Last modified: 2022-09-01
# Required environment variables:
#   INPUT_FOLDER = "<INPUT_FOLDER>"
#   VECTOR_TEMP_FOLDER = "<VECTOR_TEMP_FOLDER>"
#   INDEX = "<INDEX>"
#   SOURCE_HOST = "<SOURCE_HOST>"
#   -- ELK sink specific variables. ELK index = <INDEX> & source = "vmware_esxi_shell" & sourcetype = "vmware_esxi_artefact".
#   ELK_IP = "<IP>"
#   ELK_PORT = "<PORT>" -- ELK bulk HTTP API port. Defaults to 9200.
#   ELK_USERNAME = "<USERNAME>"
#   ELK_PASSWORD = "<PASSWORD>"
#   -- Splunk sink specific variables. Splunk index = <INDEX> & source = "vmware_esxi_shell" & sourcetype = "vmware_esxi_artefact".
#   SPLUNK_IP = "<IP>"
#   SPLUNK_PORT = "<PORT>" -- Splunk HTTP Event Collector port. Defaults to 8088.
#   SPLUNK_HEC_TOKEN = "<SPLUNK_HEC_TOKEN>" -- Splunk HEC token: https://docs.splunk.com/Documentation/Splunk/9.0.1/Data/UsetheHTTPEventCollector.

# Temporary folder used by Vector.
data_dir = "${VECTOR_TEMP_FOLDER:?A directory for Vector temporary data is required}"

[sources.VMware_ESXi_shell_input]
  type = "file"
  ignore_checkpoints = true
  include = ["${INPUT_FOLDER:?The input folder containing the Linux auth log files is required. Add '**' to process the specified folder recursively (exemple: \"C:\\logs\" or folder \"C:\\logs\\**\")}/*shell.log*"]
  read_from = "beginning"
  oldest_first = true
  fingerprint.strategy = "device_and_inode"
  max_line_bytes = 100_000_000

[transforms.VMware_ESXi_shell_parser]
  type = "remap"
  inputs = ["VMware_ESXi_shell_input"]
  source = '''
    # Match main log entries
    # "2021-05-26T21:36:27Z shell[7537454]: Interactive shell session started"
    # "2021-05-26T21:36:40Z shell[7537454]: [root]: ping X.X.X.X"
    if (match_main, err = parse_regex(.message, r'^(?P<timestamp>.*?)\s(?P<terminal>.*?)\[(?P<session>.*?)\]:\s(?P<suffix>.*)$'); err == null) {
      .timestamp = parse_timestamp!(match_main.timestamp, format: "%Y-%m-%dT%H:%M:%SZ")
      .terminal = match_main.terminal
      .session = match_main.session
      # Match command execution log entries from suffix.
      #  "[root]: ping X.X.X.X"
      if (match_suffix_command, err = parse_regex(match_main.suffix, r'\[(?P<user>.*?)\]:\s(?P<command>.*)'); err == null) {
        .operation = "execution"
        .suboperation = "esxi_execution"
        .user = match_suffix_command.user
        .command = match_suffix_command.command
      } else if (match_suffix_session_started, err = parse_regex(match_main.suffix, r'.*session started.*$'); err == null) {
        .operation = "login"
        .suboperation = "esxi_login"
        .message_suffix = match_main.suffix
      } else {
        .message_suffix = match_main.suffix
      }
    # Match other entries associated with a terminal type (such as enabling / disabling of terminal's login log entries).
    "2021-05-14T15:16:45Z SSH: SSH login enabled"
    "2021-05-14T15:10:26Z ESXShell: ESXi shell login disabled"
    } else if (match_other_terminal, err = parse_regex(.message, r'^(?P<timestamp>.*)\s(?P<terminal>.*):\s(?P<message>.*)$'); err == null) {
      .timestamp = parse_timestamp!(match_other_terminal.timestamp, format: "%Y-%m-%dT%H:%M:%SZ")
      .terminal = match_other_terminal.terminal
      .message_suffix = match_other_terminal.message
    # Match other log entries that have a timestamp.
    } else if (match_other, err = parse_regex(.message, r'^(?P<timestamp>.*)\s(?P<message>.*)$'); err == null) {
      .timestamp = parse_timestamp!(match_other.timestamp, format: "%Y-%m-%dT%H:%M:%SZ")
      .message_suffix = match_other.message
    } else {
      log("The following event is not in a valid format: " + to_string!(.message), level: "error")
    }

    # Delete irrelevant default fields
    del(.file)
    del(.message)
    del(.source_type)

    # Add the metadata fields following the parsing.
    .sourcehost = "${SOURCE_HOST:?The source host is required}"
    .host = .sourcehost
    .source = "vmware_esxi_shell"
    .sourcetype = "vmware_esxi_artefact"
  '''

# Sink to send processed logs to void, for validating that the pipeline doest not raise errors.
# [sinks.VMware_ESXi_shell_blackhole_sink]
#   type = "blackhole"
#   inputs = ["VMware_ESXi_shell_parser"]

# Sink to send processed logs to the console, for debugging.
# [sinks.VMware_ESXi_shell_console_sink]
#   type = "console"
#   inputs = ["VMware_ESXi_shell_parser"]
#   target = "stdout"
#   encoding.codec = "json"

# Sink transform to send processed logs to an ELK instance.
# [sinks.VMware_ESXi_shell_ELK_sink]
#   type = "elasticsearch"
#   inputs = ["VMware_ESXi_shell_parser"]
#   mode="bulk"
#   # bulk.action = "create"
#   bulk.index = "${INDEX:?An index name must be specified for ELK / Splunk index}"
#   endpoint = "http://${ELK_IP:?ELK IP is required}:${ELK_PORT:-9200}"
#   auth.strategy = "basic"
#   auth.user = "${ELK_USERNAME:?ELK_USERNAME is required}"
#   auth.password = "${ELK_PASSWORD:?ELK_PASSWORD is required}"
#   suppress_type_name = true # type field in header is deprecated since ELK 7.x and has been removed from ELK 8.x

# Sink transform to send processed logs to a Splunk instance.
# [sinks.VMware_ESXi_shell_Splunk_sink]
#   type = "splunk_hec_logs"
#   inputs = ["VMware_ESXi_shell_parser"]
#   endpoint = "https://${SPLUNK_IP:?Splunk IP is required}:${SPLUNK_PORT:-8088}"
#   default_token = "${SPLUNK_HEC_TOKEN:?Splunk HEC token is required}"
#   compression = "none"
#   index = "${INDEX:?An index name must be specified for the ELK / Splunk index}"
#   host_key = "host"
#   source = "{{ source }}"
#   sourcetype = "{{ sourcetype }}"
#   tls.verify_certificate = false
#   encoding.codec = "json"