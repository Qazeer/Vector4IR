# Description: Process Azure O365 Mailbox Audit logs, as retrieved by DFIR-ORC365RC.
#              Filename should match: "**/*MailboxAuditLog*.json"
# Author(s): Axel ROC, Thomas DIOT (_Qazeer)
# Version: 1.0
# Last modified: 2022-09-01
# Required environment variables:
#   INPUT_FOLDER = "<INPUT_FOLDER>"
#   VECTOR_TEMP_FOLDER = "<VECTOR_TEMP_FOLDER>"
#   INDEX = "<INDEX>"
#   SOURCE_TENANT = "<SOURCE_TENANT>"
#   -- ELK sink specific variables. ELK index = <INDEX> & source = "o365_mailboxaudit" & sourcetype = "azure_artefact".
#   ELK_IP = "<IP>"
#   ELK_PORT = "<PORT>" -- ELK bulk HTTP API port. Defaults to 9200.
#   ELK_USERNAME = "<USERNAME>"
#   ELK_PASSWORD = "<PASSWORD>"
#   -- Splunk sink specific variables. Splunk index = <INDEX> & source = "o365_mailboxaudit" & sourcetype = "azure_artefact".
#   SPLUNK_IP = "<IP>"
#   SPLUNK_PORT = "<PORT>" -- Splunk HTTP Event Collector port. Defaults to 8088.
#   SPLUNK_HEC_TOKEN = "<SPLUNK_HEC_TOKEN>" -- Splunk HEC token: https://docs.splunk.com/Documentation/Splunk/9.0.1/Data/UsetheHTTPEventCollector.

data_dir = "${VECTOR_TEMP_FOLDER:?A directory for Vector temporary data is required}"

[sources.Azure_O365_MailboxAuditLog_DFIR-O365RC_input]
  type = "file"
  include = ["${INPUT_FOLDER:?The source folder containing the DFIR-O365RC JSON outputs is required}/**/*MailboxAuditLog*.json"]
  ignore_checkpoints = true
  read_from = "beginning"
  oldest_first = true
  fingerprint.strategy = "device_and_inode"
  max_line_bytes = 100_000_000

[transforms.Azure_O365_MailboxAuditLog_DFIR-O365RC_parser]
  inputs = ["Azure_O365_MailboxAuditLog_DFIR-O365RC_input"]
  type = "remap"
  source = '''
  splitted, err = split(.message, "\n")
  if err != null {
    log("Unable to split message on newlines: " + err, level: "error")
  }

  . = flatten(map_values(splitted) -> |value| {
    parsed_value, err = parse_json(value)
    if err != null {
      log("Unable to parse JSON: " + err, level: "error")
      null
    } else {
      parsed_value
    }
  })
  '''

[transforms.Azure_O365_MailboxAuditLog_DFIR-O365RC_enrich]
  inputs = ["Azure_O365_MailboxAuditLog_DFIR-O365RC_parser"]
  type = "remap"
  source = '''
  timestamp_captured = parse_regex!(.LastAccessed, r'/Date\((?P<unix_timestamp>\d+)000\)/')
  .LastAccessedParsed, err = parse_timestamp(timestamp_captured.unix_timestamp, format: "%s")
  if err != null {
    log("Failed to parse unix timestamp: " + err, level: "error")
  }

  # Parse OperationProperties fields.
  # Expected formats:
  #   "{RuleOperation:RemoveMailboxRule, RuleId:5984694343254933505, RuleState:, RuleCondition:{}, RuleName:, RuleProvider:, RuleActions:[]}"
  #   "RuleOperation:RemoveMailboxRule, RuleId:5984694343254933505, RuleState:, RuleCondition:{}, RuleName:, RuleProvider:, RuleActions:[]"
  if is_null(.OperationProperties) == false {
    OperationProperties = .OperationProperties
    match_operation_properties, err = parse_regex(.OperationProperties, r'^\{(?P<properties>.*)\}$')
    if (err == null) {
      OperationProperties = match_operation_properties.properties
    }
    . |= parse_key_value(OperationProperties, key_value_delimiter: ":", field_delimiter: ",") ?? {}
  }

  .timestamp = .LastAccessedParsed
  .AccountName = .MailboxOwnerUPN
  .Username = .LogonUserDisplayName

  .host = "${SOURCE_TENANT:?The source AzureAD tenant is required}"
  .source = "o365_mailboxaudit"
  .sourcetype = "azure_artefact"
  '''

# Sink to send processed logs to void, for validating that the pipeline doest not raise errors.
# [sinks.Azure_O365_MailboxAuditLog_DFIR-O365RC_blackhole_sink]
#   type = "blackhole"
#   inputs = ["Azure_O365_MailboxAuditLog_DFIR-O365RC_enrich"]

# Sink to send processed logs to the console, for debugging.
# [sinks.Azure_O365_MailboxAuditLog_DFIR-O365RC_console_sink]
#   type = "console"
#   inputs = ["Azure_O365_MailboxAuditLog_DFIR-O365RC_enrich"]
#   target = "stdout"
#   encoding.codec = "json"
  
# Sink transform to send processed logs to an ELK instance.
# [sinks.Azure_O365_MailboxAuditLog_DFIR-O365RC_ELK_sink]
#   type = "elasticsearch"
#   inputs = ["Azure_O365_MailboxAuditLog_DFIR-O365RC_enrich"]
#   mode="bulk"
#   # bulk.action = "create"
#   bulk.index = "${INDEX:?An index name must be specified for ELK / Splunk index}"
#   endpoint = "http://${ELK_IP:?ELK IP is required}:${ELK_PORT:-9200}"
#   auth.strategy = "basic"
#   auth.user = "${ELK_USERNAME:?ELK_USERNAME is required}"
#   auth.password = "${ELK_PASSWORD:?ELK_PASSWORD is required}"
#   suppress_type_name = true # type field in header is deprecated since ELK 7.x and has been removed from ELK 8.x

# Sink transform to send processed logs to a Splunk instance.
# [sinks.Azure_O365_MailboxAuditLog_DFIR-O365RC_Splunk_sink]
#   type = "splunk_hec_logs"
#   inputs = ["Azure_O365_MailboxAuditLog_DFIR-O365RC_enrich"]
#   endpoint = "https://${SPLUNK_IP:?Splunk IP is required}:${SPLUNK_PORT:-8088}"
#   default_token = "${SPLUNK_HEC_TOKEN:?Splunk HEC token is required}"
#   compression = "none"
#   index = "${INDEX:?An index name must be specified for the ELK / Splunk index}"
#   host_key = "host"
#   source = "{{ source }}"
#   sourcetype = "{{ sourcetype }}"
#   tls.verify_certificate = false
#   encoding.codec = "json"